spring:
  data:
    mongodb:
      host: localhost
      port: 27019
  datasource:
    url: jdbc:postgresql://localhost:5430/demoDB
    username: usr
    password: pwd
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none